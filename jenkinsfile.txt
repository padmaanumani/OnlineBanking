pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
                sh '${MAVEN_HOME} mvn clean package'
            }
        }
    }
}
